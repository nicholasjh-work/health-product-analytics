name: health_product_analytics
version: "1.0.0"
config-version: 2

profile: health_product_analytics

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  health_product_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
